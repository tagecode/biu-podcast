electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
shamefully-hoist=true
# onlyBuiltDependencies lives in package.json as an array.
# Do NOT set only-built-dependencies here as a comma string — pnpm then
# treats it as a non-array and crashes with "onlyBuiltDependencies?.sort is not a function".

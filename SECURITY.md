# 安全策略（Security Policy）

## 支持的版本

| 版本              | 支持状态 |
| ----------------- | -------- |
| 1.x（最新稳定版） | ✅ 支持  |

仅最新稳定版接收安全更新。

## 报告漏洞

博播是本地优先的桌面应用，安全边界主要在于：

- 渲染进程安全（`sandbox` / `contextIsolation` / CSP / HTML 净化）
- 对不可信 RSS Feed / 集数 HTML 的处理
- IPC 输入的校验（zod schema）

**如果你发现安全漏洞，请不要公开提交 issue。** 请通过以下方式私密报告：

- 邮件：GitHub 用户名 `tagecode`（见仓库主页）
- 或通过 GitHub 私信联系维护者

请包含：

1. 漏洞类型与影响的模块
2. 复现步骤（最小化）
3. 平台 / 版本信息
4. 如可能的修复建议

## 处理流程

1. 确认漏洞有效性与影响范围
2. 在私密分支修复并测试
3. 发布修复版本，并在 Release 说明中致谢（除非报告者要求匿名）

## 本项目的依赖与安全

- 依赖更新由 Dependabot 跟踪（`pnpm audit`）
- 第三方原生模块（`better-sqlite3`）版本锁定
- 渲染进程仅暴露白名单式 preload API，不暴露 `ipcRenderer` 本体

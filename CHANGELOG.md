# Changelog

本项目的所有显著变更都会记录在此文件。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.2.0] - 2026-09-15

### 新增

**桌面平台补齐（P2）**
- 登录时自动启动：设置页开关（默认关闭）；Windows / macOS Login Items，Linux `~/.config/autostart`
- 拖拽 `.opml` / `.xml` 到窗口导入订阅；非法文件提示且不写库
- 复制分享链接：播客复制 Feed URL；集数优先原文链接，缺失时回退 guid / 音频 URL；复制成功显示「已复制」
- 原生右键菜单：输入框剪切/复制/粘贴/全选（含对话框）；订阅 / 集数 / 播放列表高频操作
- macOS Dock 菜单：空闲仅显示窗口；播放中可暂停/上一集/下一集

### 说明

- macOS Now Playing / 控制中心：P2 Spike 结论为继续 noop 降级（见 `mdocs/P2-6-NowPlaying-Spike.md`），不纳入本版本实现
- 刷新全部后的「发现 x 集新内容」提示可手动关闭

## [2.1.0] - 2026-08-10

### 新增

**播放链路（P1.5 收尾）**
- 播放队列持久化：App 重启后恢复队列顺序、循环模式与当前集（不自动出声）
- 集数描述时间戳跳转：`mm:ss` / `h:mm:ss` 可点击，跳到对应进度
- 章节时间轴：解析 `podcast:chapters` / `psc:chapters`，详情页展示可点击章节列表
- 播放失败分类处理：网络错误自动重试 1 次后提示重试、文件丢失提示重新下载、解码失败直接提示

**下载与存储（P1.5 收尾）**
- 播客详情页「下载全部」与播放列表「批量下载」（进入统一下载队列）
- 磁盘剩余空间预警：低于阈值（默认 500MB，可配置）时拒绝新下载并提示清理

**数据可靠性（P1.5 收尾）**
- 数据库启动自愈：损坏时从最近迁移备份恢复，无备份则保留损坏文件并以空库启动 + 通知提示

### 其他
- 多 `<enclosure>` Feed 自动选择最合适的音频源（见 `mdocs/Download-Quality-Evaluation.md`）
- 队列随「完整数据导出/导入」迁移（备份 schema v3）

## [2.0.0] - 2026-08-09

### 新增

**订阅增强（P1 阶段 1）**
- OPML 批量导入 / 导出（重复条目自动跳过）
- 暂停 / 恢复订阅（保留数据但不再拉取更新）
- 自动刷新间隔设置（30 分钟 / 1 小时 / 6 小时 / 手动）
- 新集数系统通知（刷新发现新内容时弹出）

**数据组织（P1 阶段 2）**
- 播放列表：创建 / 重命名 / 删除、添加 / 移除集数、拖拽排序
- 时间戳笔记：播放时记笔记、按集查看列表、导出 Markdown

**收听体验（P1 阶段 3）**
- 变速播放（0.5x ~ 3.0x，记忆上次选择）
- 睡眠定时器（10s ~ 60m，倒计时自动暂停）
- 播放队列 + 列表循环 / 单曲循环 / 随机播放
- 全屏播放器键盘快捷键（空格 / 方向键 / 音量键）

**桌面集成（P1 阶段 4）**
- 系统托盘：播放 / 暂停 / 上下集快捷操作，「关闭到托盘」默认开启
- 全局媒体键与自定义快捷键（设置页可录制 / 恢复默认，冲突与占用回退）
- `biu-podcast://` 深链接协议（subscribe / play 场景，三平台注册）
- 系统媒体控件：Linux MPRIS + Windows SMTC（`.NET` 伴生进程桥接），macOS 预留待接

**平台与外观（P1 阶段 5）**
- 深色 / 亮色 / 跟随系统主题
- 中 / 英双语界面 + 跟随系统语言（主进程菜单 / 托盘同步切换）
- 字体大小调节（90% / 100% / 110% / 120%）
- 应用内自动更新（启动静默检查 + 设置手动入口，GitHub Releases）
- 关于页（版本 / 开源协议 / 反馈 / 隐私说明）

**设置与管理（P1 阶段 6）**
- 存储空间占用展示（按播客统计）
- 自动清理策略（保留 7 / 30 / 90 天，预览 + 立即清理）
- 清除缓存 / 清除全部数据（二次确认后重启）
- 诊断日志（可关闭）+ 一键导出诊断信息

**其他**
- 下载历史分页加载、下载目录设置与打开

### 修复

- 托盘「显示博播」无响应：关闭窗口改为隐藏而非销毁
- 全局快捷键占用降级：候选键自动回退，冲突不致死命令
- NotesPage 遗留的 React Hook 依赖告警

### 工程

- 依赖边界校验（dependency-cruiser）接入 `pnpm lint`
- download 域覆盖率提升至 88.84%（所有核心域 ≥85%）
- 单元测试 219 例 + E2E 15 例；CI 三平台安装包自动产出

## [1.0.0] - 2026-08-04

### 新增

- MVP 完整功能主链路：
  - 订阅管理：RSS 添加 / 解析 / 去重 / 刷新 / 取消订阅
  - 播客浏览：详情页、集数列表（已听 / 已下载状态）、富文本安全渲染、标记全部已听
  - 音频播放：迷你 + 全屏播放器、上下集、进度拖拽、进度持久化、重启恢复（不自动出声）
  - 离线下载：单集下载、并发队列、暂停 / 继续 / 取消、断点续传、重启自动恢复、完整性校验
  - 本地数据：SQLite 存储、`.biubackup` 导出 / 导入（含冲突预览）
  - 桌面基础：单实例锁定、窗口状态记忆、原生菜单
- 三层测试体系：Vitest（100 用例）+ Playwright E2E（冒烟 / 黄金路径 / 续播 / 续传 / 离线播放，5 用例）
- CI 流水线：lint → 依赖边界 → typecheck → 单元 → 覆盖率 → 构建 → E2E；三平台（Win / macOS arm64+x64 / Linux）安装包自动产出
- 安全基线：`sandbox` / `contextIsolation` / CSP / HTML 净化四项，Vitest 回归

[2.0.0]: https://github.com/tagecode/biu-podcast/releases/tag/v2.0.0
[1.0.0]: https://github.com/tagecode/biu-podcast/releases/tag/v1.0.0
